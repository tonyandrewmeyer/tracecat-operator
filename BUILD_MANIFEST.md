# Build Manifest — tracecat-operator

Generated by the unattended dev-box build agent on 2026-06-22.

## Answers (Q1–Q7)

| Q | Key | Value | Source |
|---|---|---|---|
| Q1 | GitHub namespace | `tonyandrewmeyer` | default; repo `tonyandrewmeyer/tracecat-operator` returns 404 (does not exist) |
| Q2 | Juju k8s controller | `concierge-k8s` | `juju controllers` — current controller, cloud `k8s`, agent 3.6.24 |
| Q3 | Tracecat version | `1.0.0-beta.49` | latest stable beta with published images; `1.0.0-beta.50` has only RCs (image 404). Compose default is `1.0.0-beta.48` |
| Q4 | AI agent features | `yes` | default — wired disabled-by-default, enabled via `enable-agent-features=true` |
| Q5 | Auth type (day-one) | `basic` | default |
| Q6 | Superadmin email | `ops-team@example.com` | example value; stored as Juju secret `tracecat-superadmin-email` at deploy time, never in charm config |
| Q7 | Charmhub entity / track | `tracecat-k8s` / `1.0/stable` | defaults |

## Installed tool versions

| Tool | Version | Required | OK? |
|---|---|---|---|
| juju | 3.6.24 | ≥ 3.1 | ✅ |
| charmcraft | 4.2.1 | ≥ 4.0 | ✅ |
| git | 2.43.0 | — | ✅ |
| python3 | 3.12.3 | ≥ 3.12 | ✅ |
| uv | 0.11.21 | — | ✅ |
| kubectl | 1.35.5 | — | ✅ |

## Environment notes / deviations

- **GitHub push not possible in this session.** No `gh`, no `GITHUB_TOKEN`, no SSH
  keys, no git credentials are present, and the target repo does not exist. The
  build therefore proceeds against a **local git repository** (`/tmp/tracecat-operator`)
  with real commits, push-ready. To publish: install `gh` (`sudo apt install gh`),
  run `gh auth login`, `gh repo create tonyandrewmeyer/tracecat-operator --public`,
  then `git remote add origin git@github.com:tonyandrewmeyer/tracecat-operator.git`
  and `git push -u origin main`.
- **`docker` is not installed**, so the `docker manifest inspect` / `docker inspect`
  steps in the prompt were substituted with direct ghcr.io registry API checks
  (confirmed `1.0.0-beta.49` images exist for both `tracecat` and `tracecat-ui`).
- **Image tag for Q3**: the prompt recommended `1.0.0-beta.50`, but as of the
  probe only its RCs are released and no stable image is published. `1.0.0-beta.49`
  is the newest stable-beta tag with published images and is used instead.
- **Controller**: `concierge-k8s` is a k8s-cloud Juju 3.6 controller on a single
  node. It is used for live deployment smoke tests where time/resources permit.

## Image refs (authoritative — from upstream `docker-compose.yml`)

- Workload: `ghcr.io/tracecathq/tracecat:1.0.0-beta.49`
- UI: `ghcr.io/tracecathq/tracecat-ui:1.0.0-beta.49`
